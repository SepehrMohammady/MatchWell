diff --git a/node_modules/react-native-google-nearby-connection/android/build.gradle b/node_modules/react-native-google-nearby-connection/android/build.gradle
index 2fd94d5..3a293b8 100644
--- a/node_modules/react-native-google-nearby-connection/android/build.gradle
+++ b/node_modules/react-native-google-nearby-connection/android/build.gradle
@@ -1,15 +1,14 @@
 apply plugin: 'com.android.library'
 
 android {
-    compileSdkVersion 26
-    buildToolsVersion "26.0.1"
+    compileSdkVersion rootProject.ext.compileSdkVersion
+    namespace "com.butchmarshall.reactnative.google.nearby.connection"
 
     defaultConfig {
-        minSdkVersion 16
-        targetSdkVersion 26
+        minSdkVersion rootProject.ext.minSdkVersion
+        targetSdkVersion rootProject.ext.targetSdkVersion
         versionCode 1
         versionName "1.0"
-        multiDexEnabled true
     }
 
     buildTypes {
@@ -20,11 +19,9 @@ android {
 }
 
 dependencies {
-    compile fileTree(dir: 'libs', include: ['*.jar'])
+    implementation fileTree(dir: 'libs', include: ['*.jar'])
 
     // React Native
-    provided "com.facebook.react:react-native:+"
-    compile 'com.google.android.gms:play-services-nearby:11.8.0'
-    compile 'com.android.support:appcompat-v7:25.3.1'
-    compile 'com.android.support:multidex:1.0.1'
+    compileOnly "com.facebook.react:react-native:+"
+    implementation 'com.google.android.gms:play-services-nearby:19.1.0'
 }
diff --git a/node_modules/react-native-google-nearby-connection/android/src/main/java/com/butchmarshall/reactnative/google/nearby/connection/NearbyConnectionModule.java b/node_modules/react-native-google-nearby-connection/android/src/main/java/com/butchmarshall/reactnative/google/nearby/connection/NearbyConnectionModule.java
index c4090f5..675a948 100644
--- a/node_modules/react-native-google-nearby-connection/android/src/main/java/com/butchmarshall/reactnative/google/nearby/connection/NearbyConnectionModule.java
+++ b/node_modules/react-native-google-nearby-connection/android/src/main/java/com/butchmarshall/reactnative/google/nearby/connection/NearbyConnectionModule.java
@@ -44,15 +44,15 @@ import android.os.Bundle;
 import android.os.Handler;
 import android.os.Looper;
 
-import android.support.annotation.CallSuper;
-import android.support.annotation.NonNull;
-import android.support.annotation.Nullable;
-import android.support.annotation.UiThread;
-import android.support.annotation.WorkerThread;
-import android.support.v4.content.ContextCompat;
-import android.support.v4.app.ActivityCompat;
-import android.support.v4.util.SimpleArrayMap;
-import android.support.v7.app.AppCompatActivity;
+import androidx.annotation.CallSuper;
+import androidx.annotation.NonNull;
+import androidx.annotation.Nullable;
+import androidx.annotation.UiThread;
+import androidx.annotation.WorkerThread;
+import androidx.core.content.ContextCompat;
+import androidx.core.app.ActivityCompat;
+import androidx.collection.SimpleArrayMap;
+import androidx.appcompat.app.AppCompatActivity;
 import android.util.Log;
 import android.widget.Toast;
 import com.google.android.gms.common.api.Status;
@@ -399,6 +399,28 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
         return "NearbyConnection";
     }
 
+    @ReactMethod
+    public void addListener(String eventName) {
+        // Keep: Required for RN built in Event Emitter Calls.
+    }
+
+    @ReactMethod
+    public void removeListeners(Integer count) {
+        // Keep: Required for RN built in Event Emitter Calls.
+    }
+
+    /**
+     * Android 13+ (API 33) requires RECEIVER_EXPORTED or RECEIVER_NOT_EXPORTED flag.
+     * These are internal app broadcasts so we use RECEIVER_NOT_EXPORTED.
+     */
+    private void safeRegisterReceiver(BroadcastReceiver receiver, IntentFilter intentFilter) {
+        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
+            getReactApplicationContext().registerReceiver(receiver, intentFilter, Context.RECEIVER_NOT_EXPORTED);
+        } else {
+            getReactApplicationContext().registerReceiver(receiver, intentFilter);
+        }
+    }
+
     @Override
     public void onNewIntent(Intent intent) {
     }
@@ -478,7 +500,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 
 	@ReactMethod
-	protected void startAdvertising(final String endpointName, final String serviceId, final int strategy) {
+	public void startAdvertising(final String endpointName, final String serviceId, final int strategy) {
 		mIsAdvertising = true;
 		onAdvertisingStarting(endpointName, serviceId);
 
@@ -492,7 +514,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 
 		final Activity activity = getCurrentActivity();
 		final ConnectionsClient clientSingleton = getConnectionsClientSingleton(serviceId);
-		final AdvertisingOptions advertisingOptions =  new AdvertisingOptions(finalStrategy);
+		final AdvertisingOptions advertisingOptions = new AdvertisingOptions.Builder().setStrategy(finalStrategy).build();
 
         permissionsCheck(activity, Arrays.asList(getRequiredPermissions()), new Callable<Void>() {
             @Override
@@ -543,7 +565,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 
 	/** Returns {@code true} if currently advertising. */
-	@ReactMethod
+	@ReactMethod(isBlockingSynchronousMethod = true)
 	public boolean isAdvertising() {
 		return mIsAdvertising;
 	}
@@ -563,7 +585,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 
 		final Activity activity = getCurrentActivity();
 		final ConnectionsClient clientSingleton = getConnectionsClientSingleton(serviceId);
-		final DiscoveryOptions discoveryOptions = new DiscoveryOptions(finalStrategy);
+		final DiscoveryOptions discoveryOptions = new DiscoveryOptions.Builder().setStrategy(finalStrategy).build();
 
         permissionsCheck(activity, Arrays.asList(getRequiredPermissions()), new Callable<Void>() {
             @Override
@@ -610,13 +632,13 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 
 	/** Returns {@code true} if currently discovering. */
-	@ReactMethod
+	@ReactMethod(isBlockingSynchronousMethod = true)
 	public boolean isDiscovering() {
 		return mIsDiscovering;
 	}
 
 	/** Returns {@code true} if currently connecting. */
-	@ReactMethod
+	@ReactMethod(isBlockingSynchronousMethod = true)
 	public boolean isConnecting() {
 		return mIsConnecting;
 	}
@@ -656,7 +678,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerAdvertisingStartingHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.AdvertisingStarting");
-		getReactApplicationContext().registerReceiver(mAdvertisingStartingReceiver, intentFilter);
+        safeRegisterReceiver(mAdvertisingStartingReceiver, intentFilter);
 	}
 
 	/**
@@ -694,7 +716,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerAdvertisingStartedHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.AdvertisingStarted");
-		getReactApplicationContext().registerReceiver(mAdvertisingStartedReceiver, intentFilter);
+        safeRegisterReceiver(mAdvertisingStartedReceiver, intentFilter);
 	}
 
 	/**
@@ -735,7 +757,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerAdvertisingStartFailedHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.AdvertisingStartFailed");
-		getReactApplicationContext().registerReceiver(mAdvertisingStartFailedReceiver, intentFilter);
+        safeRegisterReceiver(mAdvertisingStartFailedReceiver, intentFilter);
 	}
 
 	/**
@@ -770,7 +792,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerDiscoveryStartingHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.DiscoveryStarting");
-		getReactApplicationContext().registerReceiver(mDiscoveryStartingReceiver, intentFilter);
+        safeRegisterReceiver(mDiscoveryStartingReceiver, intentFilter);
 	}
 
 	/**
@@ -805,7 +827,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerDiscoveryStartedHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.DiscoveryStarted");
-		getReactApplicationContext().registerReceiver(mDiscoveryStartedReceiver, intentFilter);
+        safeRegisterReceiver(mDiscoveryStartedReceiver, intentFilter);
 	}
 
 	/**
@@ -842,7 +864,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerDiscoveryStartFailedHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.DiscoveryStartFailed");
-		getReactApplicationContext().registerReceiver(mDiscoveryStartFailedReceiver, intentFilter);
+        safeRegisterReceiver(mDiscoveryStartFailedReceiver, intentFilter);
 	}
 
 	/**
@@ -900,7 +922,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerOnReceivePayloadHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.ReceivePayload");
-		getReactApplicationContext().registerReceiver(mOnReceivePayloadReceiver, intentFilter);
+        safeRegisterReceiver(mOnReceivePayloadReceiver, intentFilter);
 	}
 
 	/**
@@ -959,7 +981,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerOnPayloadUpdateHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.PayloadUpdate");
-		getReactApplicationContext().registerReceiver(mRegisterOnPayloadUpdateReceiver, intentFilter);
+        safeRegisterReceiver(mRegisterOnPayloadUpdateReceiver, intentFilter);
 	}
 
 	/**
@@ -1010,7 +1032,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerSendPayloadFailedHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.SendPayloadFailed");
-		getReactApplicationContext().registerReceiver(mSendPayloadFailedReceiver, intentFilter);
+        safeRegisterReceiver(mSendPayloadFailedReceiver, intentFilter);
 	}
 	
 	/**
@@ -1058,7 +1080,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerEndpointConnectionFailedHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.EndpointConnectionFailed");
-		getReactApplicationContext().registerReceiver(mEndpointConnectionFailedReceiver, intentFilter);
+        safeRegisterReceiver(mEndpointConnectionFailedReceiver, intentFilter);
 	}
 
 	/**
@@ -1110,7 +1132,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerDisconnectedFromEndpointHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.DisconnectedFromEndpoint");
-		getReactApplicationContext().registerReceiver(mDisconnectedFromEndpointReceiver, intentFilter);
+        safeRegisterReceiver(mDisconnectedFromEndpointReceiver, intentFilter);
 	}
 
 	/**
@@ -1159,7 +1181,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerConnectedToEndpointHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.ConnectedToEndpoint");
-		getReactApplicationContext().registerReceiver(mConnectedToEndpointReceiver, intentFilter);
+        safeRegisterReceiver(mConnectedToEndpointReceiver, intentFilter);
 	}
 
 	/**
@@ -1212,7 +1234,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerConnectionInitiatedToEndpointHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.ConnectionInitiatedToEndpoint");
-		getReactApplicationContext().registerReceiver(mConnectionInitiatedToEndpointReceiver, intentFilter);
+        safeRegisterReceiver(mConnectionInitiatedToEndpointReceiver, intentFilter);
 	}
 
 	/**
@@ -1257,7 +1279,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerEndpointLostHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.EndPointLost");
-		getReactApplicationContext().registerReceiver(mEndpointLostReceiver, intentFilter);
+        safeRegisterReceiver(mEndpointLostReceiver, intentFilter);
 	}
 
 	/**
@@ -1305,7 +1327,7 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	}
 	private void registerEndpointDiscoveredHandler() {
 		IntentFilter intentFilter = new IntentFilter("com.butchmarshall.reactnative.google.nearby.connection.EndPointDiscovered");
-		getReactApplicationContext().registerReceiver(mEndpointDiscoveredReceiver, intentFilter);
+        safeRegisterReceiver(mEndpointDiscoveredReceiver, intentFilter);
 	}
 
 	/**
@@ -1638,7 +1660,25 @@ public class NearbyConnectionModule extends ReactContextBaseJavaModule implement
 	 * @return All permissions required for the app connectivity to properly function.
 	 */
 	protected String[] getRequiredPermissions() {
-		return REQUIRED_PERMISSIONS;
+		java.util.List<String> perms = new java.util.ArrayList<>(java.util.Arrays.asList(REQUIRED_PERMISSIONS));
+		
+		if (android.os.Build.VERSION.SDK_INT >= 31) {
+			// On Android 12+, legacy BLUETOOTH permissions are stripped by maxSdkVersion=30
+			// If we ask for them, the OS returns DENIED and silently halts the connection process.
+			perms.remove(Manifest.permission.BLUETOOTH);
+			perms.remove(Manifest.permission.BLUETOOTH_ADMIN);
+			perms.add("android.permission.BLUETOOTH_SCAN");
+			perms.add("android.permission.BLUETOOTH_CONNECT");
+			perms.add("android.permission.BLUETOOTH_ADVERTISE");
+		}
+		if (android.os.Build.VERSION.SDK_INT >= 33) {
+			perms.add("android.permission.NEARBY_WIFI_DEVICES");
+		}
+		
+		// Some devices need Fine Location to advertise properly
+		perms.add(Manifest.permission.ACCESS_FINE_LOCATION);
+		
+		return perms.toArray(new String[0]);
 	}
 
 	/**
diff --git a/node_modules/react-native-google-nearby-connection/android/src/main/java/com/butchmarshall/reactnative/google/nearby/connection/NearbyConnectionPackage.java b/node_modules/react-native-google-nearby-connection/android/src/main/java/com/butchmarshall/reactnative/google/nearby/connection/NearbyConnectionPackage.java
index d79b2c5..b750d56 100644
--- a/node_modules/react-native-google-nearby-connection/android/src/main/java/com/butchmarshall/reactnative/google/nearby/connection/NearbyConnectionPackage.java
+++ b/node_modules/react-native-google-nearby-connection/android/src/main/java/com/butchmarshall/reactnative/google/nearby/connection/NearbyConnectionPackage.java
@@ -1,7 +1,6 @@
 package com.butchmarshall.reactnative.google.nearby.connection;
 
 import com.facebook.react.ReactPackage;
-import com.facebook.react.bridge.JavaScriptModule;
 import com.facebook.react.bridge.NativeModule;
 import com.facebook.react.bridge.ReactApplicationContext;
 import com.facebook.react.uimanager.ViewManager;
diff --git a/node_modules/react-native-google-nearby-connection/index.js b/node_modules/react-native-google-nearby-connection/index.js
index de8e3bb..fd5f9d3 100644
--- a/node_modules/react-native-google-nearby-connection/index.js
+++ b/node_modules/react-native-google-nearby-connection/index.js
@@ -26,7 +26,15 @@ import {
 	NativeModules,
 } from 'react-native';
 
-const NearbyEventEmitter = new NativeEventEmitter(NativeModules.NearbyConnection);
+const NearbyConnectionModule = NativeModules.NearbyConnection || {};
+// Fake EventEmitter since the real NativeEventEmitter crashes on RN 0.83 if module is missing
+class MockEventEmitter {
+  addListener(eventName, listener) { return { remove: () => {} }; }
+  removeListeners(count) {}
+}
+const NearbyEventEmitter = NativeModules.NearbyConnection 
+  ? new NativeEventEmitter(NativeModules.NearbyConnection) 
+  : new MockEventEmitter();
 
 export const CommonStatusCodes = {
 	"API_NOT_CONNECTED": 17,
@@ -91,12 +99,12 @@ export const Strategy = {
 class NearbyConnection {
 	// Get all the endpoints
 	static endpoints() {
-		return NativeModules.NearbyConnection.endpoints();
+		return NearbyConnectionModule.endpoints ? NearbyConnectionModule.endpoints() : Promise.resolve([]);
 	}
 
 	// Get all payloads
 	static payloads() {
-		return NativeModules.NearbyConnection.payloads();
+		return NearbyConnectionModule.payloads ? NearbyConnectionModule.payloads() : Promise.resolve([]);
 	}
 
 	// Save a file from a payload to a location
@@ -141,40 +149,40 @@ class NearbyConnection {
 
 	// Start/Stop Advertise
 	static startAdvertising(localEndpointName, serviceId, strategy = Strategy.P2P_CLUSTER) {
-		return NativeModules.NearbyConnection.startAdvertising(localEndpointName, serviceId, strategy);
+		return NearbyConnectionModule.startAdvertising ? NearbyConnectionModule.startAdvertising(localEndpointName, serviceId, strategy) : Promise.reject('Not supported');
 	}
 	static stopAdvertising(serviceId) {
-		return NativeModules.NearbyConnection.stopAdvertising(serviceId);
+		return NearbyConnectionModule.stopAdvertising ? NearbyConnectionModule.stopAdvertising(serviceId) : null;
 	}
 	static isAdvertising() {
-		return NativeModules.NearbyConnection.isAdvertising();
+		return NearbyConnectionModule.isAdvertising ? NearbyConnectionModule.isAdvertising() : Promise.resolve(false);
 	}
 
 	// Start/Stop Discover
 	static startDiscovering(serviceId, strategy = Strategy.P2P_CLUSTER) {
-		return NativeModules.NearbyConnection.startDiscovering(serviceId, strategy);
+		return NearbyConnectionModule.startDiscovering ? NearbyConnectionModule.startDiscovering(serviceId, strategy) : Promise.reject('Not supported');
 	}
 	static stopDiscovering(serviceId) {
-		return NativeModules.NearbyConnection.stopDiscovering(serviceId);
+		return NearbyConnectionModule.stopDiscovering ? NearbyConnectionModule.stopDiscovering(serviceId) : null;
 	}
 	static isDiscovering() {
-		return NativeModules.NearbyConnection.isDiscovering();
+		return NearbyConnectionModule.isDiscovering ? NearbyConnectionModule.isDiscovering() : Promise.resolve(false);
 	}
 
 	// Accept or Reject
 	static acceptConnection(serviceId, endpointId) {
-		return NativeModules.NearbyConnection.acceptConnection(serviceId, endpointId);
+		return NearbyConnectionModule.acceptConnection ? NearbyConnectionModule.acceptConnection(serviceId, endpointId) : null;
 	}
 	static rejectConnection(serviceId, endpointId) {
-		return NativeModules.NearbyConnection.rejectConnection(serviceId, endpointId);
+		return NearbyConnectionModule.rejectConnection ? NearbyConnectionModule.rejectConnection(serviceId, endpointId) : null;
 	}
 
 	// Connect or Disconnect
 	static connectToEndpoint(serviceId, endpointId) {
-		return NativeModules.NearbyConnection.connectToEndpoint(serviceId, endpointId);
+		return NearbyConnectionModule.connectToEndpoint ? NearbyConnectionModule.connectToEndpoint(serviceId, endpointId) : null;
 	}
 	static disconnectFromEndpoint(serviceId, endpointId) {
-		return NativeModules.NearbyConnection.disconnectFromEndpoint(serviceId, endpointId);
+		return NearbyConnectionModule.disconnectFromEndpoint ? NearbyConnectionModule.disconnectFromEndpoint(serviceId, endpointId) : null;
 	}
 	
 	// ------------------------------------------------------------------------
